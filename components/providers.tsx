'use client'

// ThemeProvider is now in app/layout.tsx
// This file is kept for any future client-side providers

export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
