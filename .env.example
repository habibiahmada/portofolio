# Environment Variables Template
# Copy this to .env.local and fill in your values
# ⚠️  NEVER commit .env.local to git!

# Supabase Configuration
# Get these from https://app.supabase.com → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
NEXT_PUBLIC_SITE_URL=https://www.habibiahmada.dev

# Database (direct for seeding)
# Get from Supabase → Project Settings → Database → Connection Pooler
POSTGRES_URL=your_postgres_url

# Admin Configuration
# Exact emails only (comma-separated). Must match Supabase Auth user email.
ADMIN_ALLOWED_EMAILS=your_admin_email@example.com

# Agent blog API (POST /api/agent/blog)
# Generate with: openssl rand -base64 32  (>= 32 random bytes)
# Shared secret between this app and agent-hub; never commit the real value.
AGENT_BLOG_TOKEN=your_agent_blog_token

# Review window before auto-publish (default 20 minutes for daily cadence)
BLOG_REVIEW_MINUTES=20
# Legacy fallback if minutes unset:
# BLOG_REVIEW_HOURS=24

# Optional: Test bypass key (REMOVE in production!)
TEST_BYPASS_KEY=test-bypass-secret-123
